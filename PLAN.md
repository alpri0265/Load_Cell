# План проекту: Ваговий модуль на базі Load Cell 2000 кг

## 1. Мета проекту
Створити автономний ваговий пристрій з тензодатчиком (load cell) на 2000 кг, дисплеєм LCD2004 та керуванням меню через поворотний енкодер (тип EC11). Пристрій вимірює вагу/навантаження, дозволяє калібрування, тарування та вибір одиниць виміру через зручне меню. Проект самостійний, без інтеграції з іншими системами.

## 2. Компоненти (BOM)

| Компонент | Призначення | Примітка |
|---|---|---|
| Тензодатчик (load cell) 2000 кг | Первинний датчик сили | S-подібний або бруско-подібний, 4-провідний, ~2 мВ/В |
| HX711 | 24-бітний АЦП для тензодатчика | Обов'язковий підсилювач/АЦП, живлення 5В |
| Arduino Nano | Керуючий контролер | ATmega328P |
| LCD 2004 (20x4) | Індикація | Рекомендовано з I2C-перехідником (PCF8574), економить піни |
| Енкодер EC11 (модуль) | Керування меню | Виводи модуля: `5V - KEY - S2 - S1 - GND` |
| EEPROM (вбудована в Nano) | Зберігання калібрування | Використовується штатна EEPROM ATmega328P |
| Джерело живлення 5В (стабілізоване) | Живлення всього вузла | Бажано окремий стабільний регулятор, тензодатчики чутливі до шуму живлення |
| Корпус, клемники, кабель екранований | Механіка/монтаж | Екранований кабель для сигналу з датчика обов'язковий |

**Примітка щодо EC11:** використовується готовий модуль із 5 виводами: `5V` (живлення), `KEY` (кнопка натискання), `S2` і `S1` (фази квадратурного обертання, аналог CLK/DT), `GND`. Логіка обробки — стандартне квадратурне декодування S1/S2 + окрема кнопка KEY з дебаунсом.

## 3. Розподіл виводів Arduino Nano

| Пристрій | Пін Nano | Коментар |
|---|---|---|
| HX711 DOUT | D6 | Опитування (бібліотека HX711 не потребує переривання) |
| HX711 SCK | D7 | |
| LCD2004 (I2C) SDA | A4 | Апаратний I2C |
| LCD2004 (I2C) SCL | A5 | Апаратний I2C |
| Енкодер S1 | D2 | Апаратне переривання (INT0) — для точного підрахунку кроків |
| Енкодер S2 | D3 | Апаратне переривання (INT1) або читання в тому ж ISR |
| Енкодер KEY (кнопка) | D5 | Із підтяжкою `INPUT_PULLUP`, програмний дебаунс |
| Енкодер GND | GND | |
| Енкодер 5V | 5V | |
| (опційно) Реле/зумер перевантаження | D8 | Якщо буде потрібен сигнал тривоги |

Живлення HX711 і LCD — від стабілізованих 5В; за наявності шумів на лінії живлення рекомендується окремий LDO-регулятор і феритові кільця на кабелі датчика.

### Живлення від зовнішнього блока 5В (замість USB)

- Подавати живлення потрібно на пін **`5V`** Arduino Nano, а НЕ на `VIN` — `VIN` іде через бортовий стабілізатор, розрахований на 7–12В, і з рівних 5В на виході дасть занижену, нестабільну напругу.
- Пін `5V` — це вихід бортового стабілізатора Nano без захисту від перенапруги/переплутаної полярності, тому подавайте туди лише перевірений, стабільний 5В±5% (4.75–5.25В).
- **Ніколи не підключайте одночасно USB-кабель і зовнішній блок живлення до `5V`** — це може призвести до конфлікту двох джерел напруги і пошкодження USB-порту комп'ютера/мікроконтролера. Під час роботи від зовнішнього БЖ USB відключати.
- Спільна земля (`GND`) блока живлення, Nano, HX711, LCD і енкодера — обов'язкова (одна точка з'єднання, «зіркою», щоб уникнути земляних петель).
- Бажано додати конденсатори по живленню біля Nano та HX711: керамічний 100 нФ + електролітичний 100–470 мкФ між `5V` і `GND` — зменшує шум АЦП HX711 від імпульсного БЖ.
- Струм споживання всього вузла невеликий (Nano + HX711 + LCD2004 з підсвіткою + енкодер ≈ 100–150 мА) — підійде будь-який БЖ на 5В/500мА і вище.

## 4. Архітектура прошивки

```
main.cpp
 ├── ScaleModule      — робота з HX711: читання, фільтрація, тарування, калібрування
 ├── DisplayModule     — вивід на LCD2004 (I2C), формування екранів
 ├── EncoderModule     — обробка обертання (ISR) + кнопки (дебаунс)
 ├── MenuModule        — стан-машина меню, навігація, редагування значень
 └── StorageModule     — збереження/зчитування калібрувального коефіцієнта, тари, одиниць у EEPROM
```

### Стан-машина меню
- **MAIN** — головний екран: поточна вага, одиниці, статус (стабільно/нестабільно), значок тари
- **MENU** — список пунктів (навігація обертанням, вибір коротким натиском)
  - Тарування (Tare)
  - Калібрування (майстер із введенням еталонної ваги)
  - Одиниці виміру (кг / г / lb)
  - Фільтрація (кількість усереднень / поріг стабільності)
  - Підсвітка/контраст дисплея
  - Скидання до заводських налаштувань
  - Інфо про прошивку
- **EDIT** — редагування числового значення обертанням, підтвердження кнопкою
- Вихід у MAIN — довге утримання кнопки або пункт "Назад"

## 5. Ключові алгоритми

- **Фільтрація сигналу**: ковзне середнє або медіанний фільтр на N останніх відліків HX711 (типово 10–20) для зменшення шуму; детекція стабільності (зміна < порогу протягом X мс) для індикатора "стабільно".
- **Тарування**: збереження поточного "нульового" сирого значення, віднімання при обчисленні ваги.
- **Калібрування**: 
  1. Тарування без навантаження.
  2. Встановлення еталонної ваги (сертифікованої або відомої маси, бажано ≥30% від макс. діапазону).
  3. Введення її значення через меню/енкодер.
  4. Обчислення коефіцієнта `scale = (raw_с_вагою − raw_нуль) / відома_вага`.
  5. Збереження коефіцієнта в EEPROM.
- **Зберігання**: структура з калібрувальним коефіцієнтом, тарою (опційно), вибраними одиницями та контрольною сумою/маркером валідності в EEPROM.

## 6. Етапи розробки

1. **Макетування апаратної частини** — збірка на макетній платі, перевірка живлення, екранування кабелю датчика.
2. **Базовий тест HX711** — читання сирих значень у Serial Monitor, перевірка стабільності сигналу без навантаження.
3. **Підключення LCD2004 (I2C)** — тестовий вивід, підбір адреси I2C (типово 0x27 або 0x3F), перевірка контрасту.
4. **Підключення енкодера** — тест підрахунку кроків та натискань кнопки через переривання.
5. **Інтеграція: відображення ваги на LCD** — без меню, лише головний екран.
6. **Реалізація меню та стан-машини** — навігація, вибір пунктів.
7. **Майстер калібрування та тарування** — повний цикл із збереженням в EEPROM.
8. **Фільтрація та індикація стабільності**.
9. **Механічний монтаж датчика** — правильне навантаження без бокових/крутильних сил, перевірка лінійності на різних вагах.
10. **Польове калібрування та перевірка точності** — еталонні ваги в кількох точках діапазону (наприклад, 10%, 50%, 90% від 2000 кг).
11. **(Опційно) Розширення** — див. розділ 8.

## 7. Тестування

- Перевірка нуля після тривалого простою (дрейф).
- Перевірка лінійності: похибка в кількох контрольних точках діапазону.
- Тест стабільності меню при швидкому обертанні енкодера (відсутність пропущених кроків/брязку контактів).
- Перевірка збереження калібрування після перезавантаження/відключення живлення.

## 8. Опційні розширення (на майбутнє)

- Реле або зумер сигналізації перевантаження/недовантаження.
- Логування на SD-картку.
- Живлення від акумулятора з індикатором заряду.
- Друга мова інтерфейсу меню.

## 9. Відкриті питання (потребують вашого рішення)

- Чи є вимоги до точності/класу датчика (наприклад, C3) та повірки?
